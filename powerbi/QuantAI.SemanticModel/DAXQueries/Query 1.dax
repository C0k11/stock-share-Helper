EVALUATE ADDCOLUMNS(TOPN(5, VALUES(dim_date[date]), dim_date[date], ASC), "prices_rows", CALCULATE(COUNTROWS(fact_prices)), "news_rows", CALCULATE(COUNTROWS(fact_news)), "signals_rows", CALCULATE(COUNTROWS(fact_signals)), "odds_rows", CALCULATE(COUNTROWS(fact_event_odds)))